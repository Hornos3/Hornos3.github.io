$border-radius-inner   = 20px 20px 20px 20px;
$border-radius         = 20px;
$content-desktop       = 700px;
$content-desktop-large = 1050px;

  body{
    background:url("../images/background.jpg");
    background-repeat: no-repeat;
    background-attachment: fixed;
    background-position: 50% 50%;
    background-size: cover
  }

  .post-expand .post-title-link{
    color: #222
  }

  .post-block{
    background: #ffffff
    border-radius: initial
    box-shadow: 0 2px 2px 0 rgba(0,0,0,0.8), 0 3px 1px -2px rgbx(0,0,0,0.8), 0 1px 5px 0 rgba(0,0,0,0.8);
    padding: 40px
  }

//博客内容透明化
//文章内容的透明度设置
//.content-wrap {
//  opacity: 0.8;
//}
//.main-inner {
//   margin-top: 60px;
//   padding: 60px 60px 60px 60px;
//}

.post-block {
	background: rgba(255,248,220,0.7) none repeat scroll !important;
}

//侧边框的透明度设置
//.sidebar {
//  opacity: 0.8;
//}
.sidebar-inner {
  background: rgba(149,255,255,0.7) none repeat scroll !important;
  }


//菜单栏的透明度设置
.header-inner {
  background: rgba(255,255,255,0.8);
}

//搜索框（local-search）的透明度设置
.popup {
  opacity: 0.8;
}
